<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head') %>
  <body>
    <ul>
      <% if (message){  %>
          <li>
            <p><%= message.text %></p>
            <p><%= message.author %></p>
            <p><%= message.created_at %></p>
          </li>
      <% } %>
    </ul>
  </body>
</html>
